
const version = '1.0.0';

function getVersion(): string {
  return version;
}

function init(): string {
  return version;
}

function getNetList():Array<string> {
  return [];
}

export default { init, getVersion, getNetList };
